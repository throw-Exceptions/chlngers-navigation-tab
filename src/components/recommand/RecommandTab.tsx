import React from 'react';
import Screen from '@components/common/Screen';

const RecommandTab:React.FC = () => {
  return (
    <Screen>
      추천 탭
    </Screen>
  );
};

export default RecommandTab;
